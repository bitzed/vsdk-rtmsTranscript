{
  "name": "rtms-videosdk-minimal",
  "version": "1.0.0",
  "description": "Minimal sample: detect session.rtms_started and record transcription via RTMS",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@zoom/rtms": "latest",
    "dotenv": "^16.0.0",
    "express": "^4.18.0"
  },
  "engines": {
    "node": ">=20.3.0"
  }
}
